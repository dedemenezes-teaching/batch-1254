console.log('herminone');

// check dat type
console.log(typeof ('hermione'));
console.log(typeof (12));


// tyypeof([]) // returns object v


// primitive types => string number boolean undefined
// complex types => object array function

const beatles = ["paul", "john", "ringo", "george"];

beatles.forEach((beatle) => {
  console.log(beatle).toUpperCase();
});

function circleArea(radius) {
  // let area = :??
  // instrunction
  //
  //
  //
  //
  //
  //
  return area
}

circleArea(4);

const square = (x) => {
  return x * x;
};
